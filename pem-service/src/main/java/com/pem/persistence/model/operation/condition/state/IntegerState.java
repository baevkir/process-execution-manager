package com.pem.persistence.model.operation.condition.state;

public class IntegerState extends AbstractState<Integer> {
    @Override
    public String toString() {
        return "IntegerState{} " + super.toString();
    }
}
